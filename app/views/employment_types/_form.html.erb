<%= form_with(model: employment_type) do |form| %>
  <% if employment_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(employment_type.errors.count, "error") %> prohibited this employment_type from being saved:</h2>

      <ul>
        <% employment_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.textarea :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
