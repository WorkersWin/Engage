<%= form_with(model: state) do |form| %>
  <% if state.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(state.errors.count, "error") %> prohibited this state from being saved:</h2>

      <ul>
        <% state.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :country, class: "label" %>
  <div class="control">
  <div class="select">
    <%= collection_select(:state, :country_id, Country.all.order(:name), :id, :name) %>
  </div>
  </div>
  </div>

  <div class="field">
    <%= form.label :name, class: "label" %>
  <div class="control">
    <%= form.text_field :name, class: "input" %>
  </div>
  </div>

  <div class="field">
    <%= form.label :usps, class: "label" %>
  <div class="control">
    <%= form.text_field :usps, class: "input" %>
  </div>
  </div>

  <div class="field">
  <div class="control">
    <%= form.submit "Submit", class: "button is-link" %>
  </div>
  </div>
<% end %>
