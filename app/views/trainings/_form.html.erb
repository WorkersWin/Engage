<%= form_with(model: training) do |form| %>
  <% if training.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(training.errors.count, "error") %> prohibited this training from being saved:</h2>

      <ul>
        <% training.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, autofocus: true, placeholder: "Training Name" %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description, placeholder: "Training Description" %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
