<%= form_with(model: job_title) do |form| %>
  <% if job_title.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(job_title.errors.count, "error") %> prohibited this job_title from being saved:</h2>

      <ul>
        <% job_title.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title, class: "label" %>
  <div class="control">
    <%= form.text_field :title, class: "input" %>
  </div>
  </div>

  <div class="field">
    <%= form.label :description, class: "label" %>
  <div class="control">
    <%= form.text_area :description, class: "textarea" %>
  </div>
  </div>

  <div class="field">
  <div class="control">
    <%= form.submit "Submit", class: "button is-link"%>
  </div>
  </div>
<% end %>
